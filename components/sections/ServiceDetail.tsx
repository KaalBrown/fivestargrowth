import { Facebook, Instagram, MapPin, MonitorSmartphone, Rocket, Star } from "lucide-react";
import { Button } from "@/components/ui/Button";
import { serviceByKey, type ServiceKey } from "@/lib/services";

const icons = { website: MonitorSmartphone, trust: MapPin, growth: Rocket, social: Instagram };

export function ServiceVisual({ serviceKey }: { serviceKey: ServiceKey }) {
  const service = serviceByKey[serviceKey];
  const Icon = icons[serviceKey];
  return <div className="relative overflow-hidden border border-white/15 bg-[#101010] p-6 text-white shadow-[12px_12px_0_#101010] sm:p-9"><div className="absolute -right-12 -top-14 h-44 w-44 rounded-full blur-3xl" style={{ backgroundColor: service.colour, opacity: 0.42 }} /><div className="relative flex min-h-52 flex-col justify-between"><div className="flex items-start justify-between"><span className="fsg-mono text-[10px] uppercase tracking-[.16em] text-white/60">Five Star Growth / service</span><Star className="h-4 w-4" style={{ color: service.colour }} fill="currentColor" /></div><div className="flex items-end justify-between gap-5"><Icon className="h-20 w-20 stroke-[1.25] sm:h-24 sm:w-24" style={{ color: service.colour }} /><div className="max-w-40 border-l border-white/20 pl-4 text-right text-xs leading-5 text-white/75">Built around visible proof, clear actions, and sustainable local growth.</div></div></div></div>;
}

export function ServiceDetailPage({ serviceKey }: { serviceKey: ServiceKey }) {
  const service = serviceByKey[serviceKey];
  return <main className="bg-[#f7f7f3] text-[#101010]"><section className="border-b border-black/15 px-5 py-16 lg:px-9 lg:py-24"><div className="mx-auto grid max-w-[1240px] items-center gap-10 lg:grid-cols-[1.05fr_.95fr]"><div><p className="fsg-mono text-[10px] uppercase tracking-[.16em]" style={{ color: service.colour }}>{service.eyebrow}</p><h1 className="mt-5 max-w-3xl text-5xl font-extrabold leading-[.94] tracking-[-.07em] sm:text-6xl">{service.title}</h1><p className="mt-6 max-w-xl text-lg leading-8 text-black/70">{service.summary}</p><Button href={`/contact?reason=video-audit&cta=service-${service.key}&service=${service.key}`} variant="glow" className="mt-8">Request Free Video Audit</Button></div><ServiceVisual serviceKey={serviceKey} /></div></section><section className="px-5 py-16 lg:px-9 lg:py-24"><div className="mx-auto max-w-[1240px]"><div className="grid gap-4 lg:grid-cols-2"><article className="border border-black/15 p-7"><p className="fsg-mono text-[10px] uppercase tracking-[.16em]" style={{ color: service.colour }}>Who it is for</p><p className="mt-5 text-xl font-bold leading-8 tracking-[-.04em]">{service.whoItsFor}</p></article><article className="border border-black/15 p-7"><p className="fsg-mono text-[10px] uppercase tracking-[.16em]" style={{ color: service.colour }}>The problem it solves</p><p className="mt-5 text-xl font-bold leading-8 tracking-[-.04em]">{service.problem}</p></article></div><div className="mt-16 grid gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><p className="fsg-mono text-[10px] uppercase tracking-[.16em]" style={{ color: service.colour }}>Exactly what is included</p><h2 className="mt-4 text-4xl font-extrabold leading-[.95] tracking-[-.06em]">A focused system, not a vague retainer.</h2></div><ul className="grid gap-3 sm:grid-cols-2">{service.includes.map((item, index) => <li key={item} className="border border-black/15 p-5 text-sm leading-6"><span className="mr-3 font-extrabold" style={{ color: service.colour }}>0{index + 1}</span>{item}</li>)}</ul></div><div className="mt-20 border border-black/15 bg-[#e7e4dd] p-6 sm:p-8"><div className="grid gap-5 md:grid-cols-2"><div className="border border-black/15 bg-[#f7f7f3] p-6"><p className="text-xs font-extrabold uppercase tracking-[.12em] text-[#101010]/70">Before</p><p className="mt-3 text-xl font-bold leading-7 text-[#101010]">{service.before}</p></div><div className="border p-6 text-white" style={{ borderColor: service.colour, backgroundColor: "#101010" }}><p className="text-xs font-extrabold uppercase tracking-[.12em]" style={{ color: service.colour }}>After</p><p className="mt-3 text-xl font-bold leading-7">{service.after}</p></div></div></div><div className="mt-20 grid gap-10 lg:grid-cols-[1fr_.8fr]"><div><p className="fsg-mono text-[10px] uppercase tracking-[.16em]" style={{ color: service.colour }}>FAQ</p><div className="mt-4 border-y border-black/15">{service.faqs.map((faq) => <details key={faq.question} className="group border-b border-black/15 py-5 last:border-0"><summary className="cursor-pointer list-none pr-7 text-lg font-extrabold marker:hidden">{faq.question}<span className="float-right transition group-open:rotate-45">+</span></summary><p className="mt-3 max-w-2xl text-sm leading-6 text-black/65">{faq.answer}</p></details>)}</div></div><aside className="self-end bg-[#101010] p-7 text-white"><p className="text-2xl font-extrabold leading-tight tracking-[-.04em]">Want a clearer view of your best next move?</p><p className="mt-4 text-sm leading-6 text-white/70">We will review your current online presence and send practical priorities for your business.</p><Button href={`/contact?reason=video-audit&cta=service-faq-${service.key}&service=${service.key}`} variant="glow" className="mt-7 w-full">Request Free Video Audit</Button></aside></div></div></section></main>;
}

export function SocialMarks() { return <div className="flex items-center gap-3 text-white/70"><Facebook className="h-5 w-5" /><Instagram className="h-5 w-5" /><span className="text-[10px] font-extrabold tracking-[.12em]">META</span></div>; }
export function ServicesCta({ href = "/contact?reason=video-audit&cta=services" }: { href?: string }) { return <Button href={href} variant="glow">Request Free Video Audit</Button>; }
